# Terraform
.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Plans — may contain sensitive values
tfplan
tfplan.*
plan.txt
plan.json

# Local overrides (actual tfvars tracked only via *.example)
terraform.tfvars
terraform.tfvars.json
*.auto.tfvars
*.auto.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Generated Lambda zip (built by archive_file data source)
**/.lambda.zip

# Claude local config
.claude/settings.local.json

# OS
.DS_Store
